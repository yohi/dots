# ===================================================================
# Zsh Configuration Example
# ===================================================================
# このファイルをコピーして config.zsh として使用してください
# cp config.example.zsh config.zsh

# ===================================================================
# DOTFILES_DIR のカスタム設定
# ===================================================================
# dotfilesリポジトリのルートディレクトリを指定
# 未設定の場合、自動検出が試みられます
#
# DOTFILES_DIR="$HOME/dotfiles"

# ===================================================================
# 関数読み込みの設定
# ===================================================================
# 関数ディレクトリのサブパス
FUNCTIONS_SUBDIR="zsh/functions"

# デバッグモード (true/false)
# true: 関数読み込み時に詳細情報を表示
# false: サイレントモード（デフォルト）
FUNCTIONS_DEBUG=${ZSH_FUNCTIONS_DEBUG:-false}

# 読み込み対象ファイルパターン
FUNCTIONS_PATTERN="**/*.zsh"

# ===================================================================
# スキップするファイルパターン
# ===================================================================
# これらのパターンに一致するファイルは読み込まれません
FUNCTIONS_SKIP_PATTERNS=(
    "*.broken"      # 破損したファイル
    "*.disabled"    # 無効化されたファイル
    "*.tmp"         # 一時ファイル
    "*.backup"      # バックアップファイル
    "*~"            # エディタのバックアップ
)

# ===================================================================
# 代替検索ディレクトリ（優先度順）
# ===================================================================
# DOTFILES_DIRが環境変数で未設定の場合、
# これらのディレクトリから順に dotfiles ルートを自動検出します
CANDIDATE_DIRS=(
    "$HOME/.dotfiles"
    "$HOME/dotfiles"
    "$HOME/.config/dotfiles"
    "$HOME/dots"
    "$HOME/.dots"
)

# ===================================================================
# パフォーマンス設定
# ===================================================================
# 関数の遅延読み込みを有効化（実験的）
# true: 関数が呼ばれた時に初めて読み込む（高速化）
# false: 起動時にすべて読み込む（デフォルト）
# FUNCTIONS_LAZY_LOAD=false

# ===================================================================
# AWS関連設定
# ===================================================================
# デフォルトAWSプロファイル
# export AWS_PROFILE="default"

# AWSリージョン
# export AWS_DEFAULT_REGION="ap-northeast-1"

# ===================================================================
# エディタ設定
# ===================================================================
# これらは zshrc で設定されるため、通常は不要です
# export VISUAL=nvim
# export EDITOR=nvim

# ===================================================================
# 開発環境設定
# ===================================================================
# Go言語
# export GOPATH=$HOME/.go
# export PATH=$PATH:$GOPATH/bin

# Node.js (nodenv)
# export PATH="$HOME/.nodenv/shims:${PATH}"

# Python (pyenv)
# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$PYENV_ROOT/bin:$PATH"

# ===================================================================
# プライバシー設定
# ===================================================================
# Claude Code/Cursor IDE での履歴無効化
# export DISABLE_CLAUDE_CODE_HISTORY=1

# テレメトリー無効化
# export DISABLE_TELEMETRY=1
# export DISABLE_ERROR_REPORTING=1

# ===================================================================
# カスタマイズ例
# ===================================================================

# 例1: カスタムエイリアス
# alias myproject="cd ~/projects/myproject && code ."

# 例2: 環境変数
# export MY_CUSTOM_VAR="value"

# 例3: カスタム関数
# my_function() {
#     echo "Hello from custom function"
# }

# ===================================================================
# 注意事項
# ===================================================================
# - このファイルは .gitignore に追加することを推奨します
# - 機密情報（APIキー、パスワード等）は含めないでください
# - 変更後は新しいシェルを開くか、source ~/.zshrc を実行してください
