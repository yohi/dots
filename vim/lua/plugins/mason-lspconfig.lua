-- This file is now obsolete as we use Neovim's built-in LSP API
-- mason-lspconfig.nvim is no longer needed with vim.lsp.config()

return {
    -- mason-lspconfig.nvim is no longer needed
    -- LSP configuration is handled directly via vim.lsp.config() in lsp_cfg.lua
}
