-- This file is now obsolete as we use Neovim's built-in LSP API
-- LSP configuration is handled in lsp.lua and lsp_cfg.lua using vim.lsp.config()

return {
    -- nvim-lspconfig is no longer needed with Neovim's built-in LSP API
    -- Configuration moved to lsp.lua and lsp_cfg.lua
}
